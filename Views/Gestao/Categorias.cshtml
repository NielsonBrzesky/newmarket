@model IEnumerable<newmarket.Models.Categoria>
@{
    Layout = "_LayoutAdmin";
}
<h2>Categorias</h2>
<hr>
<a asp-action="NovaCategoria" asp-controller="Gestao" class="btn btn-success">Criar nova categoria</a>
<hr/>

<script>
    $(document).ready(function() {
        $('#idCategorias').DataTable();
    });
</script>

<table id="idCategorias" class="table table-striped table-bordered">
    <thead>
        <tr>
            <th>Id</th>
            <th>Nome</th>
            <th>Ações</th>
        </tr>
    </thead>
    <body>
        @foreach (var categorias in Model)
        {
            <tr>
                <td>@categorias.Id</td>
                <td>@categorias.Nome</td>
                <td>
                    <a class="btn btn-primary" href="/Gestao/EditarCategoria/@categorias.Id">Editar</a>
                    <button class="btn btn-danger">Deletar</button>
                </td>
            </tr>
        }
    </body>
</table>

